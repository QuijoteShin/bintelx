# config/upstream.conf
# bintelx.cifrid.com — backend API + realtime

upstream api_backend {
    server unix:/run/php/php8.4-fpm-bintelx.sock;
}

# Shared realtime services
include /var/www/bintelx.cifrid.com/bintelx/config/server/snippets/upstreams-realtime.conf;

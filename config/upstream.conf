# config/upstream.conf
# bintelx.cifrid.com — upstreams específicos del proyecto
# Upstreams compartidos (channel, webrtc) en bintelx/config/server/snippets/upstreams-realtime.conf

upstream api_backend {
    server unix:/run/php/php8.4-fpm-bintelx.sock;
}

# config/upstream.conf
# bintelx.cifrid.com — backend API only
# Realtime upstreams loaded globally from nginx.global.conf

upstream api_backend {
    server unix:/run/php/php8.4-fpm-bintelx.sock;
}
